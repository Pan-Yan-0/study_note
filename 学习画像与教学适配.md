# 潘岩：学习画像与教学适配

更新：2026-09-20。用于本项目教学，不是能力测验或人格评价。

## 来源与可信范围

本轮完整读取《IMDB情感分析学习笔记_对话整理.docx》的 195 个段落、5 个表格，无内嵌图片。原始对话日期未知；导入日期不代表对话发生日期。段落定位见 [.skill-review/qwen-imdb-extracted.txt](.skill-review/qwen-imdb-extracted.txt)；表格在提取稿末尾，编号与源文件一致。

文档已区分“我的问题”和“回答摘要”，但它仍是整理稿而非原始聊天导出。以下用户证据均指“整理稿中归为用户的问题”，没有独立核对原始消息。第十节 P0168 是导出请求，P0169 却称实际问题为 BERT，存在错配，不能据此认定用户提出过具体 BERT 问题或已经理解该节。

最新导师、学习目标与项目安排以 AGENTS.md 和当前会话为准。本次不重新认证课程掌握，不把私人对话归档自动外发到 GitHub。

## 核心判断

你已经会把接触过的模型组织成一条解释链，也会主动暴露不确定处。当前最值得补的桥梁，是把“语言任务的直觉”连接到“具体对象如何计算”，再连接到“损失如何让参数改变”。不能简单概括为没有基础，也不能从长篇自我解释推定已经会独立实现。

| 观察证据 | 支持的判断与边界 | 教学调整 |
|---|---|---|
| P0064 主动解释 word→id、词向量、CNN 与 LSTM，并追问特征工程和模型选择的边界 | 已有整体任务意识和局部表征概念；仍把部分门控过度绑定情感词义 | 保留其正确部分，拆开任务、表示、结构、学习目标、训练阶段 |
| P0107 将遗忘门描述成“遗忘多少”的权重 | 已出现门控比例直觉，不能继续仅以早期 good/not 误解代表全部理解 | 从逐维数值向量接到门公式，检查门值与模型参数的区别 |
| P0107 明确因吴恩达教学常讲神经元，希望看到每个神经元/隐藏层的数据传递 | 明确的解释粒度偏好；不能命名为固定的“视觉型学习者” | 一个坐标的计算→向量→批量张量→代码；图中标时间轴与层轴 |
| P0107 问 h、c、输出门如何传给下一层；P0128 问激活函数到底在哪里 | 时间递推、层间传播、逐元素操作之间尚有连接缺口 | 每条箭头标来源、去向、shape；不要只给抽象架构框图 |
| P0128 把注意力理解成给 h 新增一列分数，并要求“具体到各个数据” | 能形成可检验假设；当前假设混淆新张量与拼接 | 用小 states 实算独立 scores、归一化、广播与求和 |
| P0128 明确说尚未深入学反向传播；P0107 用小数连乘理解梯度问题 | 有变化率直觉，但完整链式法则与参数更新未获证据 | 小型前向→loss→局部梯度→一次更新，再联系状态通路 |
| P0034 说只修改 epoch 并询问是否太大，列出多模型提交结果 | 至少报告过运行/调参经历；不能由此确认独立实现或最佳配置 | 学习曲线、验证集选 checkpoint、一次只改变关键条件 |
| P0144 说“几乎已经理解” | 主观清晰感有所提高；没有后续独立作答记录 | 记为已接触/自评理解，后续用一个新变式验证，不重新整套考试 |

“容易追求完全通透才继续”来自 AI 的 P0157 评价，仅凭本稿不足以给用户贴该标签。可观察事实是用户多次问理解深度，并希望知道研究生需要掌握到什么程度。

## 后续采用的教学方式

先给一行全图定位，再只放大当前卡点。例如：

```text
数据：评论 → token IDs + 有效位置
模型：Embedding → 编码表示 → 汇聚 → logits
推理：logits → 类别/概率
训练：logits + 标签 → loss → 梯度 → 更新参数
```

讲机制时尽量沿同一个小例子，不在词义比喻、公式和 PyTorch shape 之间突然换对象。每一步回答四件事：输入是什么、计算了什么、输出是什么、哪些数会在训练中更新。图示与数值是帮助定位的工具，不要求每次回答都画完整网络。

对长篇自我解释，先摘出正确理解，再只纠正最先影响后续推理的一处。避免一句“基本正确”放过关键误解，也避免用一整篇新教程覆盖用户问题。

## 最近的补桥与验收（无固定时间）

以下是候选教学任务，不是当前要求马上完成的考试，也不新增一门完整前置课程。

| 补桥 | 交付物 | 验收标准 |
|---|---|---|
| 计算对象与训练闭环 | 一个小线性层加激活的计算表，和一次更新记录 | 分清输入、参数、激活、loss、梯度；说明 backward 与 step 的职责 |
| LSTM 的两条状态路径 | 两个时间步的简图，标 h/c；必要时加第二层 | 分清时间步与层，说明门是向量；能指出“遇到 not 必然清空”并非规则 |
| 注意力汇聚 | 3 个 token、每个 2 个特征的数值表 | scores 独立产生；权重沿 token 归一化；汇聚后每条评论保留 2 个特征；能解释 PAD 为何需处理 |
| 实验判断 | 一次运行的训练/验证曲线与 checkpoint 选择理由 | 根据验证指标选模型，不凭一次榜单下降断定原因；未知时列出待验证解释 |

完成必要补桥即可继续 Transformer/BERT；完整 LSTM 推导不是门槛。已经能做的项可以快过。下一次教学优先以一个小计算任务确定当前连接缺口，不根据历史稿要求从零重学。

## 原 AI 回答中不应继承的结论

这些是材料问题，不算用户的错误；本节也不代表对原稿所有技术主张逐条认证。

| 原稿位置与说法 | 应采用的边界 |
|---|---|
| P0036–38：典型过拟合、5 轮左右最佳 | 两组提交不足以确定原因或最佳点；应检查训练/验证曲线与实验条件。[验证曲线官方说明](https://scikit-learn.org/stable/modules/learning_curve.html) |
| P0112：只有 h 传出，c 永远不离开该层 | h/c 都沿时间传递；层间通常传 h；接口返回最终 h/c。“传出”要指定路径。[PyTorch LSTM](https://docs.pytorch.org/docs/stable/generated/torch.nn.LSTM.html) |
| P0100–101、P0139：tanh 导数恒小于 1，加法让梯度畅通 | tanh 导数可等于 1；梯度受权重与多条路径影响；LSTM 的加权状态通路缓解问题但不保证不消失。直接 c 路径的 f 不能冒充完整总导数。[D2L LSTM](https://zh.d2l.ai/chapter_recurrent-modern/lstm.html) |
| P0082 / 表3：but 附近遗忘门降低保留或清空 | 未观察具体模型门值，不能把词与门行为写成确定映射 |
| P0119：Attention 全部跑完才开始；表5：Attention 都压成向量 | 只能描述具体的序列汇聚结构；自注意力等结构并不遵循这个统一模板 |
| P0159：加 mask 可能直接提 0.5–1 点 | 先判断实现是否需要及正确使用 mask；没有对照实验不许承诺收益 |
| P0163：同种子多跑看方差 | 同种子用于复现检查；估计不同初始化等带来的波动应采用多个种子 |
| P0115 / P0140：研究生必须默写门公式，Transformer 推导暂不值得 | 深度依当前任务选择；用户主线是 Transformer/BERT，应学相关计算，而不是按模型名称统一决定背诵门槛 |

## 本轮持久化结果

- skill 2.1.0：补充历史聊天证据归属、计算粒度切换、机制解释边界和实验因果判断。
- AGENTS.md：加入本画像入口及最新证据摘要；不覆盖课程完成记录。
- 私人对话与个人画像不进入可移植 skill 包；通用规则不携带姓名、导师或成绩列表。
- 需要后续真实作答才能确认教学适配是否有效，本轮不声称已经改善长期学习效果。
